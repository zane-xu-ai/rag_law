# 边界切分首尾完整性：宪法.md
## 分析过程

1. 使用与 `chunking.boundary.iter_text_slices_boundary_aware` 一致的步骤：先 `iter_text_slices` 得滑窗初值 `(s0,e0)`，再 `adjust_start` / `adjust_end`（`max_probe`），并对起点施加 `_clamp_start_for_overlap`（重叠下界 `overlap_floor`，默认等于 `chunk_overlap`）。
2. 对每一块检查：`s==0` 或 `text[s-1]` 为句界则**首完整**；`e==len(text)` 或 `text[e-1]` 为句界则**尾完整**（句界见下方 `BOUNDARY_CHARS`）。
3. 本报告由 `scripts/analyze_boundary_chunk_integrity.py` 生成；相邻块实际重叠长度为 `上一块 char_end - 本块 char_start`（与预览 `overlap_between_adjacent` 一致）。

- **生成时间（UTC）**：`2026-04-06 06:20:25Z`
- **生成命令**：`PYTHONPATH=src uv run python scripts/analyze_boundary_chunk_integrity.py data/宪法.md --chunk-size 1500 --overlap 100 --overlap-floor 60 --max-probe 30 -o doc/chunk/宪法_边界切分_CHUNK1500_O100_floor60.md`

## 参数
- 文件字符数：`18836`
- `chunk_size`=1500, `chunk_overlap`=100, `overlap_floor`=60, `max_probe`=30
- 句界字符集：`'\n\r。！；？'`（不含逗号、顿号）

## 判定

- **首完整**：`s==0` 或 `text[s-1]` 属于句界字符（块从句界之后开始）。
- **尾完整**：`e==len(text)` 或 `text[e-1]` 属于句界字符。

## 汇总

| 指标 | 数量 |
| --- | ---: |
| chunk 总数 | 14 |
| 首不完整 | 3 |
| 尾不完整 | 1 |
| 首尾均不完整 | 0 |
| 发生过对齐回退（fallback）的块 | 0 |
| 起点被重叠上界左移（clamp）的块 | 0 |

## 相邻块实际重叠

- 各相邻对重叠长度：`[100, 96, 96, 97, 131, 123, 90, 118, 72, 110, 110, 144, 61]`
- min=61，max=144，avg=103.69（单位：字符）
- 与 `overlap_floor=60` 的关系：实际重叠 **≥** 该下界；若句界对齐后自然重叠更大，则不会为凑目标而右移起点。

## 不完整 chunk 明细与原因

### chunk 索引 0（全局切片 `[12, 1500)`）

- 首完整：True；尾完整：False
- fallback：False；重叠导致起点左移：False
- 首不完整原因：—
- 尾不完整原因：块末为逗号/顿号：当前句界集合不含「，」「、」，语义上常为半句，但规则判定为尾不完整

块首预览：

```text
\n(1982 年 12 月 4 日第五届全国人民代表大会第五次会议通过 1982 年 12 月 4 日全国人民代表大会公告 公布施行 根据 1988 年 4 月
```

块尾预览：

```text
展社会主义民主,健全社会主义法治,贯彻新发展理念,自力更生,艰苦奋斗,逐步 实现工业、农业、国防和科学技术的现代化,推动物质文明、政治文明、精神文明、社会文明、
```

### chunk 索引 1（全局切片 `[1400, 2896)`）

- 首完整：False；尾完整：True
- fallback：False；重叠导致起点左移：False
- 首不完整原因：前一字符为 '主'：±30 内未找到更合适的句界起点，或平局策略保留当前位置
- 尾不完整原因：—

块首预览：

```text
义的各项制度,发展社会主义 市场经济,发展社会主义民主,健全社会主义法治,贯彻新发展理念,自力更生,艰苦奋斗,逐步 实现工业、农业、国防和科学技术的现代化,推动
```

块尾预览：

```text
机关、检察机关都由人民代表大会产生,对它负责,受它监督。\n\n 中央和地方的国家机构职权的划分,遵循在中央的统一领导下,充分发挥地方的主动性、积极 性的原则。\n\n
```

### chunk 索引 4（全局切片 `[5600, 7128)`）

- 首完整：False；尾完整：True
- fallback：False；重叠导致起点左移：False
- 首不完整原因：前一字符为 '体'：±30 内未找到更合适的句界起点，或平局策略保留当前位置
- 尾不完整原因：—

块首预览：

```text
主义和国际主义、共产主义的教育,进行辩证唯物主义和历史唯物主 义的教育,反对资本主义的、封建主义的和其他的腐朽思想。\n\n第二十五条 国家推行计划生育,使人口的增
```

块尾预览：

```text
。除因国家安全或者追查 刑事犯罪的需要,由公安机关或者检察机关依照法律规定的程序对通信进行检查外,任何组织或者 个人不得以任何理由侵犯公民的通信自由和通信秘密。
```

### chunk 索引 11（全局切片 `[15400, 16915)`）

- 首完整：False；尾完整：True
- fallback：False；重叠导致起点左移：False
- 首不完整原因：前一字符为 '民'：±30 内未找到更合适的句界起点，或平局策略保留当前位置
- 尾不完整原因：—

块首预览：

```text
法院和人民检察院的工作;撤销本级人民 政府的不适当的决定和命令;撤销下一级人民代表大会的不适当的决议;依照法律规定的权限决定 国家机关工作人员的任免;在本级人民
```

块尾预览：

```text
 第一百一十七条 民族自治地方的自治机关有管理地方财政的自治权。凡是依照国家财政体制 属于民族自治地方的财政收入,都应当由民族自治地方的自治机关自主地安排使用。
```

