# 边界切分首尾完整性：宪法.md
## 分析过程

1. 使用与 `chunking.boundary.iter_text_slices_boundary_aware` 一致的步骤：先 `iter_text_slices` 得滑窗初值 `(s0,e0)`，再 `adjust_start_extended` / `adjust_end_extended`（强→弱→初值，扩展扫描），重叠夹紧后再对句首做二次扩展对齐与协调；重叠区间 `[overlap_floor, overlap_ceiling]`（默认均等于 `chunk_overlap`）。
2. 对每一块检查：`s==0` 或 `text[s-1]` 为句界则**首完整**；`e==len(text)` 或 `text[e-1]` 为句界则**尾完整**（句界见下方 `BOUNDARY_CHARS`）。
3. 本报告由 `scripts/analyze_boundary_chunk_integrity.py` 生成；相邻块实际重叠长度为 `上一块 char_end - 本块 char_start`（与预览 `overlap_between_adjacent` 一致）。

- **生成时间（UTC）**：`2026-04-06 07:49:18Z`
- **生成命令**：`PYTHONPATH=src uv run python scripts/analyze_boundary_chunk_integrity.py data/宪法.md --chunk-size 1500 --overlap 50 --overlap-floor 40 --overlap-ceiling 160 --max-probe 30 --clamp-adjust-max-rounds 2 -o doc/chunk/宪法_边界切分完整性统计.md`

## 参数
- 文件字符数：`18836`
- `chunk_size`=1500, `chunk_overlap`=50, `overlap_floor`=40, `overlap_ceiling`=160, `max_probe`=30, `max_boundary_scan`=800, `boundary_priority_overlap`=False, `clamp_adjust_max_rounds`=2
- 句界字符集：`'\n\r。！；？'`（不含逗号、顿号）

## 判定

- **首完整**：`s==0` 或 `text[s-1]` 属于句界字符（块从句界之后开始）。
- **尾完整**：`e==len(text)` 或 `text[e-1]` 属于句界字符。

## 汇总

| 指标 | 数量 |
| --- | ---: |
| chunk 总数 | 13 |
| 首不完整 | 3 |
| 尾不完整 | 2 |
| 首尾均不完整 | 0 |
| 发生过对齐回退（fallback）的块 | 0 |
| 起点被重叠上界左移（clamp）的块 | 0 |

## 相邻块实际重叠

- 各相邻对重叠长度：`[50, 60, 45, 44, 50, 52, 55, 52, 62, 59, 45, 49]`
- min=44，max=62，avg=51.92（单位：字符）
- 与 `overlap_floor=40`、`overlap_ceiling=160` 的关系：实际重叠应在 **[40, 160]** 内（起点经句界对齐后由 `_clamp_start_to_overlap_range` 夹紧）；若窗口内无法同时满足句界与区间，可能回退初值或产生更短块。

## 不完整 chunk 明细与原因

### chunk 索引 0（全局切片 `[0, 1500)`）

- 首完整：True；尾完整：False
- fallback：False；重叠导致起点左移：False
- 首不完整原因：—
- 尾不完整原因：块末为逗号/顿号：当前句界集合不含「，」「、」，语义上常为半句，但规则判定为尾不完整

块首预览：

```text
# 中华人民共和国宪法\n\n(1982 年 12 月 4 日第五届全国人民代表大会第五次会议通过 1982 年 12 月 4 日全国人民代表大会公告 公布施行 根
```

块尾预览：

```text
展社会主义民主,健全社会主义法治,贯彻新发展理念,自力更生,艰苦奋斗,逐步 实现工业、农业、国防和科学技术的现代化,推动物质文明、政治文明、精神文明、社会文明、
```

### chunk 索引 1（全局切片 `[1450, 2956)`）

- 首完整：False；尾完整：True
- fallback：False；重叠导致起点左移：False
- 首不完整原因：前一字符为 ','：±30 内未找到更合适的句界起点，或平局策略保留当前位置
- 尾不完整原因：—

块首预览：

```text
艰苦奋斗,逐步 实现工业、农业、国防和科学技术的现代化,推动物质文明、政治文明、精神文明、社会文明、生 态文明协调发展,把我国建设成为富强民主文明和谐美丽的社会
```

块尾预览：

```text
分发挥地方的主动性、积极 性的原则。\n\n 第四条 中华人民共和国各民族一律平等。国家保障各少数民族的合法的权利和利益,维护和 发展各民族的平等团结互助和谐关系。
```

### chunk 索引 2（全局切片 `[2896, 4396)`）

- 首完整：True；尾完整：False
- fallback：False；重叠导致起点左移：False
- 首不完整原因：—
- 尾不完整原因：块末字符为 ' '：adjust_end 在 ±30 内未找到句界，或平局策略未推到句号等

块首预览：

```text
 第四条 中华人民共和国各民族一律平等。国家保障各少数民族的合法的权利和利益,维护和 发展各民族的平等团结互助和谐关系。禁止对任何民族的歧视和压迫,禁止破坏民族
```

块尾预览：

```text
的需要,可以依照法律规定对公民的私有财产实行征收或者征用并给予补 偿。\n\n 第十四条 国家通过提高劳动者的积极性和技术水平,推广先进的科学技术,完善经济管理体 
```

### chunk 索引 8（全局切片 `[11599, 13108)`）

- 首完整：False；尾完整：True
- fallback：False；重叠导致起点左移：False
- 首不完整原因：前一字符为空白，句界判定可能跨行/格式导致
- 尾不完整原因：—

块首预览：

```text
员会组成人员在常务委员会开会期间,有权依照法律规定的程序提出对国务院或者国务院各部、各 委员会的质询案。受质询的机关必须负责答复。\n\n 第七十四条 全国人民代表
```

块尾预览：

```text
人民代表大会常务委员会提出议案;\n\n- (三)规定各部和各委员会的任务和职责,统一领导各部和各委员会的工作,并且领导不属于 各部和各委员会的全国性的行政工作;\n
```

### chunk 索引 10（全局切片 `[14498, 15991)`）

- 首完整：False；尾完整：True
- fallback：False；重叠导致起点左移：False
- 首不完整原因：前一字符为逗号/顿号：不在 BOUNDARY_CHARS（仅含 。！？；与换行）
- 尾不完整原因：—

块首预览：

```text
行政法规的遵守和执 行;依照法律规定的权限,通过和发布决议,审查和决定地方的经济建设、文化建设和公共事业建 设的计划。\n\n 县级以上的地方各级人民代表大会审查和
```

块尾预览：

```text
当的决定。\n\n 第一百零九条 县级以上的地方各级人民政府设立审计机关。地方各级审计机关依照法律规定 独立行使审计监督权,对本级人民政府和上一级审计机关负责。\n\n
```

